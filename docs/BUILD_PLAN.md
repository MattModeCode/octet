# Octet — build plan

Living roadmap for the staged build. **Read this file first in every new chat** — it tells you exactly where the build stands. One stage per chat: read this file + the briefs, decompose into subagents, integrate, verify, update this file, report done, stop.

Full stage rationale lives in the original Claude Code plan; this file is the working checklist + handoff notes that survive context resets.

---

## Status

**Current stage: 7 (M4 — Online core) — not started. Blocked on a dev Firebase project + web config, not available in this environment; see "Risks/dependencies" below.**
**Post-Stage-6 fidelity pass (global hitbox fix + full-game mockup rebuild: Main Menu, Song Select, Gameplay HUD, Results, Calibration, Editor audit) — COMPLETE and verified. See its own section below, right before Stage 7.**
**Stage 6 (M3 — Automatic analysis) — COMPLETE and verified. Also landed in this stage: the Editor shell is no longer blocked — see below — and a button-hitbox bug (confined to the editor) is fixed. Its one remaining carryover, the Gameplay HUD import, landed in a follow-up session — see the Stage 3 HUD note below.**
**Stage 5 (M2b — Note editing, QOL, save/export, playtest) — COMPLETE and verified.**
**Stage 4 (M2a — Editor shell, audio import, waveform, timing) — COMPLETE. The "editor-shell-from-1A blocked" note below was WRONG — see Stage 6's handoff for the correction; the shell is now built from the real imported mockup (option 2a).**
**Stage 3 (M1b — HUD, results, calibration, song select) — IN PROGRESS. Non-HUD parts (results, calibration, song select, gameplay flow) COMPLETE and verified. HUD-from-2A import not yet attempted with the *correct* Design MCP access pattern (see Stage 6 correction below) — worth revisiting, this may no longer be blocked either.**
**Stage 2 (M1a — Core gameplay systems) — COMPLETE, verified headless under Godot 4.7.**
**Stage 1 (M0 — Conductor clock & one-lane vertical slice) — COMPLETE, verified headless under Godot 4.7.**
**Stage 0 (Foundation & design system) — COMPLETE, verified headless under Godot 4.7.**

Godot install: `C:\Users\1chin\OneDrive\Desktop\Godot_v4.7-stable_win64.exe` (not on PATH — pass the full path to bash, or add it to PATH). Verified via:
```
"<path-to-godot>" --headless --quit --path .          # project loads clean
"<path-to-godot>" --headless -s tests/run_tests.gd --path .   # test suite
```

---

## Operating rules (every stage)

- **No version control actions** — never `git init/add/commit/push`, no auto-commit hooks. Matthew commits himself.
- **Design system is `docs/DESIGN_BRIEF.md`** — Octet palette/type only, never MashuAI. Everything reads `core/design_tokens.gd` (the `DesignTokens` autoload) — never hard-code hex in scenes/scripts.
- **Tunables live in `config/*.tres`** via the `Config` autoload (static tuning: gameplay/scoring) or `SettingsStore` autoload (per-machine: calibration offsets, accessibility, scroll speed) — not scattered through code.
- **Conductor clock is central** (Stage 1 builds it) — all timing/judgment derives from audio playback position + latency/calibration offsets, never frame delta.
- **`.oct`/`.octet` formats exactly** per PROJECT_BRIEF §4 — locked, don't redesign.
- **Answer-first, terse, autonomous, Canadian spelling, sentence case.**

## Cross-chat protocol

1. Read this file, `CLAUDE.md`, `docs/PROJECT_BRIEF.md`, `docs/DESIGN_BRIEF.md`, and (HUD/editor stages) `docs/DESIGN_HANDOFF.md`.
2. Confirm the stage's entry preconditions below.
3. Decompose into the listed subagents, run independent slices in parallel, integrate on the main thread yourself (don't let parallel agents fight over shared files like `project.godot`).
4. Run headless verification (`godot --headless --quit` + the test runner) and any manual-in-Godot checks.
5. Update this file: tick the checklist, add/refresh handoff notes, flip the status line.
6. Report done to the user with the handoff summary. Stop.

---

## Stage 0 — Foundation & design system — ✅ COMPLETE

### What's built
- `project.godot` + folder layout: `game/ editor/ audio/ net/ ui/ core/ native/ assets/ config/ tests/`.
- **Design tokens**: `core/design_tokens.gd` (`DesignTokens` autoload) — full palette, mirrored lane colours (0–7: orchid/pink/coral/amber/amber/coral/pink/orchid), corner radii, font family names, `lane_color(i)` helper. `assets/theme/octet_theme.tres` (Theme resource: Button + Panel styles, hairline borders, pink focus glow). `assets/fonts/font_{ui,display,mono}.tres` — `SystemFont` fallbacks (Inter/Space Grotesk/JetBrains Mono by name, generic fallback); **no real font binaries yet** — see `assets/fonts/README.md` for the swap-in procedure when `.ttf` files are sourced (SIL OFL licensed).
- **Chart data model**: `core/chart.gd` (`Chart`) + `core/{timing_point,chart_note,chart_metadata,chart_audio}.gd` — one `class_name` per file (GDScript requires this; the original task brief assumed one file, which isn't possible). `core/oct_io.gd` (`OctIO.load_oct/save_oct`) — round-trip verified against the exact PROJECT_BRIEF §4.1 example. `core/octet_bundle.gd` (`OctetBundle`) — `read_manifest()` fully works; `write_bundle()` is a documented stub (manifest only) pending Stage 5.
- **Config**: `core/{gameplay,scoring,settings}_config.gd` + `config/{gameplay,scoring,settings}.tres` with PROJECT_BRIEF §2.4–2.6/§2.9 default values. `core/config.gd` (`Config` autoload, loads gameplay/scoring at `_ready()`). `core/settings_store.gd` (`SettingsStore` autoload, persists `SettingsConfig` to `user://settings.tres`). `core/keybind_defaults.gd` (`KeybindDefaults.DEFAULT_LANE_KEYS`, A S D F J K L ;).
- **Shell**: `ui/scene_router.gd` (`SceneRouter` autoload — `goto_scene`, `goto_scene_pushed`/`go_back`, real implementation not a stub). `audio/conductor.gd` (`Conductor` — placeholder, `get_song_time_ms()` stub returning 0.0, Stage 1 replaces). `net/net_client.gd` (`Net` — placeholder, `is_online()` stub, Stage 7 replaces). `ui/main.tscn` + `ui/main.gd` — themed placeholder main menu (wordmark, ambient pulse, Play/Editor/Browse maps/Profile buttons routing to not-yet-built scenes with graceful missing-scene errors).
- `assets/icons/icon.svg` — minimal placeholder (referenced by `project.godot`, was missing until integration pass).
- **Headless test runner**: `tests/run_tests.gd` (`TestRunner`, `extends SceneTree`) + `tests/test_oct_io.gd` (3 tests) + `tests/test_config_load.gd` (2 tests). All 5 pass.

### Gotchas found during integration (read before writing more GDScript/tests)

1. **`Callable` bound to a `RefCounted` does NOT keep it alive.** It resolves the target by `ObjectID` at call time, not a strong reference. If you build a `Callable` from a local `RefCounted` instance and let that local variable go out of scope, the callable later fails with `Attempt to call function '...' on a null instance`. Fix: keep a persistent strong reference (e.g. an array on the owning object) for as long as the callable might be invoked. See `TestRunner._suites` in `tests/run_tests.gd`.
2. **`class_name`-declared scripts are eagerly compiled into the global class cache before `[autoload]` singletons are registered**, under `godot --headless -s`. A *static* identifier reference to an autoload (e.g. `Config.gameplay`) inside a `class_name` script fails to compile: `Identifier not found: Config`. Fix: dynamic lookup via `TestRunner.get_autoload("Config")` (walks `Engine.get_main_loop().root`) instead of a bare static reference, in any `class_name`-declared script that needs an autoload. Regular (non-`class_name`) scripts loaded through the normal scene/autoload boot path (confirmed via `--headless --quit`) do not hit this — it's specific to the `-s` test-runner entry point's compile ordering.
3. **Autoload `_ready()` has not run yet at `_initialize()` time** under `godot --headless -s` (the node exists under the tree root, but its `_ready()` — where `Config` loads its `.tres` resources — hasn't fired). Fix: `await process_frame` once at the top of `_initialize()` before touching any autoload state. Already applied in `tests/run_tests.gd`.

None of the three fixes above touch gameplay code — they're specific to the `-s` headless test-runner path. `godot --headless --quit` (normal boot through `run/main_scene`) had zero errors from the start.

### Verification run (Godot 4.7.stable)
```
Ran 5 test(s): 5 passed, 0 failed.
```
`godot --headless --quit --path .` — clean, no autoload/resource errors.

### What Stage 1 assumes
- `Conductor` autoload exists at `res://audio/conductor.gd` with a stub `get_song_time_ms()` — replace its contents with the real conductor clock (§6.2), keep the autoload registration in `project.godot` as-is.
- `Config.gameplay` / `KeybindDefaults.DEFAULT_LANE_KEYS` are available for timing windows and default key bindings.
- `SettingsStore.settings` is available for calibration offset storage (audio_offset_ms/input_offset_ms fields already exist on `SettingsConfig`).
- If Stage 1 adds more headless tests referencing autoloads, apply gotchas #2 and #3 above from the start.

---

## Stage 1 — M0: Conductor clock & one-lane vertical slice — ✅ COMPLETE

### What's built
- **Conductor clock** (`audio/conductor.gd`, real implementation replacing the Stage 0 stub): owns an `AudioStreamPlayer` child. `song_time_ms()` = pure static `compute_song_time_ms(raw_stream_sec, output_latency_sec, audio_offset_ms)` (`(raw - latency) * 1000 + offset`), fed live from `player.get_playback_position() + AudioServer.get_time_since_last_mix()` and `AudioServer.get_output_latency()`, clamped monotonic non-decreasing while playing (guards against mix-timing jitter stepping the clock backward). `judgment_error_ms(tap_ms, target_ms, input_offset_ms)` = `(tap_ms + input_offset_ms) - target_ms`. **Sign convention** (documented in the file): both calibration offsets are pure additive, "positive = shift the corresponding clock later" — simplest self-consistent convention, matches e.g. osu!'s universal-offset model. Reads `SettingsStore.settings.audio_offset_ms`/`input_offset_ms` defensively (safe if `SettingsStore` isn't ready). `play/stop/pause/resume/seek_ms/is_playing` round out the instance API.
- **Lane input** (`core/lane_input.gd`, new `LaneInput` autoload, registered in `project.godot` *after* `SettingsStore`): registers InputMap actions `lane_0`..`lane_7` from `SettingsStore.settings.lane_keys` (new field on `SettingsConfig`, empty = fall back to `KeybindDefaults.DEFAULT_LANE_KEYS`, so existing saved settings stay valid with no migration). `binding_for(lane)`, `current_key_string(lane)`, `rebind(lane, keycode)` (persists via `SettingsStore.save()`). Uses `OS.find_keycode_from_string` / `OS.get_keycode_string` to convert between the `KeybindDefaults` key-string convention and Godot's `Key` enum.
- **Rebind panel foundation** (`ui/rebind_panel.tscn` + `.gd`): builds 8 rows (lane-colour swatch via `DesignTokens.lane_color`, label, current-key button) procedurally in `_ready()`; click a key button then press a key to rebind via `LaneInput.rebind`. Foundation only, not the final settings screen (that's Stage 3, §2.8).
- **Vertical slice** (`game/vertical_slice.tscn` + `.gd`): one lane (index 0), one note, a judgment line, driven entirely by `Conductor.song_time_ms()` — `note_y = judgment_y - (target_ms - song_time_ms) * PIXELS_PER_MS * scroll_speed`. `scroll_speed` (from `SettingsStore.settings.scroll_speed`) scales approach distance only; judgment math never reads it. Hit detection on the `lane_N` action bucket `abs(judgment_error_ms(...))` against `Config.gameplay.window_perfect_ms/great_ms/good_ms` → Perfect/Great/Good/Miss, shown with signed ms error (early/late). Auto-Miss if a note goes unhit past `window_good_ms + MISS_GRACE_MS`.
- **Fixture** (`game/fixture_click_track.gd`, `FixtureClickTrack`): procedurally builds a mono 16-bit `AudioStreamWAV` click track in memory (4 bars @ 120 BPM default) — no committed audio binary, fully reproducible. `beat_target_ms(beat_index, bpm)` gives the alignment anchor the slice uses for its note's target time.
- **Tests**: `tests/test_conductor.gd` (`TestConductor`) — 5 cases covering `compute_song_time_ms` and `judgment_error_ms` (basic, zero-offset, late, early, on-time-with-offset), calling the statics via `load("res://audio/conductor.gd")`. `tests/test_lane_input.gd` (`TestLaneInput`) — 2 cases confirming all 8 actions register and `rebind` actually swaps the bound key (restores the original binding afterward so it doesn't leave global state mutated). Both registered in `tests/run_tests.gd._register_all_tests()`.

### Gotcha found during integration (read before adding more `class_name` scripts)

**A brand-new `class_name`-declared script is invisible to both `godot --headless --quit` and `godot --headless -s` until the editor's global script-class cache (`.godot/global_script_class_cache.cfg`) has been regenerated at least once.** Adding `tests/test_conductor.gd` (`TestConductor`), `tests/test_lane_input.gd` (`TestLaneInput`), and `game/fixture_click_track.gd` (`FixtureClickTrack`) and referencing them from `tests/run_tests.gd` initially failed with `Identifier "TestConductor" not declared in the current scope` under `-s`, even though `--headless --quit` had already run clean. Fix: run `godot --headless --editor --quit --path .` once after adding/renaming any `class_name` script — this forces the "Registering global classes..." editor pass and rewrites the cache file — then the normal headless entry points pick the new class up. Not needed for non-`class_name` scripts (autoloads like `LaneInput`, plain scene scripts like `vertical_slice.gd`) since those are resolved by path, not by the global class cache.

### Design decision worth carrying to the vault (per CLAUDE.md §1/§3)
Calibration offset sign convention was deliberately simplified to "both offsets are pure additive, positive = shift later" rather than trying to encode an asymmetric "forgive hardware lateness" story per-offset — the asymmetric framing didn't actually hold up under the additive formula and would have been a silent correctness bug. Keep this convention when Stage 3 builds the real calibration screen; the tap-to-the-beat routine should compute `stored_offset = -average_measured_error` (or however it's derived) with this additive convention in mind, not the other way around.

### Verification run (Godot 4.7.stable)
```
Ran 12 test(s): 12 passed, 0 failed.
```
`godot --headless --quit --path .` — clean. In-editor smoke test (`--headless --path . res://game/vertical_slice.tscn --quit-after 60` and same for `res://ui/rebind_panel.tscn`) — both run without script errors; full manual in-editor play (F6) still recommended before Stage 2 sign-off if not already done live.

### What Stage 2 assumes
- `Config.gameplay` timing windows and `Conductor.judgment_error_ms`/`song_time_ms` are the judgment primitives — Stage 2's 8-lane note/scoring systems should call these directly rather than re-deriving timing math.
- `LaneInput.binding_for(lane)` / `current_key_string(lane)` are the lane-input surface; extend, don't duplicate, when wiring all 8 lanes.
- `game/vertical_slice.gd` is a disposable M0 proving ground, not a component to extend in place — Stage 2 builds the real multi-lane note/scoring scene fresh, reusing the same Conductor/LaneInput/Config calls it validated.

## Stage 2 — M1a: Core gameplay systems — ✅ COMPLETE

### What's built
- **Judgment/scoring engine** (`game/judge_engine.gd`, `JudgeEngine`): pure, time-driven core, deliberately decoupled from rendering/real input/the live Conductor — driven by explicit `update(song_time_ms)` + `on_lane_press/release(lane, song_time_ms)`, mirroring Stage 1's "pure math behind a Node" split (`Conductor.compute_song_time_ms`). Handles tap, chord (multiple `ChartNote`s sharing `time_ms`, no separate type — matches the `.oct` model as-is), and hold (head judged like a tap; ticks every `hold_tick_interval_ms` while held, credited to accuracy/score only; tail judged like a tap against `end_time_ms` at release time — an early release naturally buckets to Miss via the same window logic, no special-cased branch needed). Exposes `score`, `combo`/`max_combo`, `accuracy()`, `health`, `judgment_counts`, `hit_errors` (ms, signed — ready for Stage 3's histogram), `grade()`, `is_full_combo()`/`is_all_perfect()`, `is_failed()`/`is_ranked()`, `current_multiplier()`, and signals `judged`/`combo_changed`/`health_changed`/`song_failed` for HUD wiring.
- **`game/judgment.gd`** (`Judgment.Kind` enum + `bucket/weight/health_delta/display_name`) and **`game/grading.gd`** (`Grading.grade_for` + full-combo/all-Perfect badge checks) — single source of truth for the window→judgment and judgment→weight/health/grade mappings, all reading `Config.gameplay` thresholds.
- **`game/gameplay_mods.gd`** (`GameplayMods`): `no_fail`/`practice` value object; No-Fail disables the fail flag and flags the run unranked (`is_ranked() == false`). Practice-mode playback behaviour (arbitrary start point, reduced rate) is plumbing-only, deferred.
- **Two new tunables** on `GameplayConfig`/`config/gameplay.tres`: `combo_multiplier_step: int = 10` (multiplier = `1 + floor(combo / step)`, capped at the existing `combo_multiplier_cap`) and `hold_tick_interval_ms: float = 100.0`.
- **`game/play_field.tscn`/`.gd`** — minimal 8-lane playable scene (logic-first, minimal visuals per the stage brief): loads the fixture chart via `OctIO.load_oct`, builds a `JudgeEngine` against `Config.gameplay`/`Config.scoring`, plays `FixtureClickTrack.build()` through `Conductor`, feeds `LaneInput`-bound presses/releases into the engine, positions falling note rects the same way `vertical_slice.gd` did (now for all 8 lanes, holds drawn as a stretched rect from head to tail), and shows score/combo/accuracy/health/grade as plain `Label`s. Note visuals hide on a pure time threshold (past the note's — or hold tail's — Good window) rather than correlating back to the `judged` signal, since by definition that note has necessarily been resolved (hit or auto-Missed) by then.
- **Fixture** `tests/fixtures/m1a_fixture.oct` (new, committed): a chord (lanes 0 & 4 @ 1000ms), a tap (lane 2 @ 1500ms), and a hold (lane 3, 2000-2250ms) — small enough to auto-play exactly in tests, aligned to `FixtureClickTrack`'s 120 BPM clicks for manual play_field verification.
- **Tests** `tests/test_gameplay.gd` (`TestGameplay`, 9 cases, registered in `run_tests.gd`): all-Perfect run, one missed note, chord hit/missed, hold held-fully vs. early-release (exact score/combo/accuracy/health arithmetic asserted), No-Fail draining health to 0 without failing/ranking, grade-threshold boundaries, and one full round-trip loading the committed fixture through the real `OctIO` path. Unlike `TestConductor`/`TestLaneInput`, this suite needs no `TestRunner.get_autoload()` at all — `JudgeEngine`/`GameplayConfig`/`ScoringConfig`/`GameplayMods`/`Chart`/`ChartNote` are plain classes with zero autoload dependency, so fresh instances reproduce the `.tres` defaults directly.

### Gotchas found during integration
1. **GDScript's static type checker treats the global `floor()` built-in's return as `Variant`, not `float`**, because it's overloaded across int/float/vector — `var steps := floor(x)` fails to compile under this project's "warnings as errors" setting with "The variable type is being inferred from a Variant value." Fix: use the type-specific global `floorf(x: float) -> float` instead of `floor()` when the result is immediately assigned with `:=`. Same likely applies to `ceil`/`round` — prefer `ceilf`/`roundf` inside `:=` assignments.
2. Same class-cache gotcha as Stage 1 (`godot --headless --editor --quit` after adding new `class_name` scripts) applied again for `Judgment`, `Grading`, `GameplayMods`, `JudgeEngine`, `TestGameplay`.

### Design decisions worth carrying to the vault (per CLAUDE.md §1/§3)
- **Hold ticks intentionally do not touch combo or health** — only accuracy and score. This keeps "combo" meaning "notes hit in a row" (head/tail/tap events only) rather than a per-100ms counter during holds, and keeps health tied to discrete note outcomes. Flagged as a simplification to revisit at playtest if holds feel under- or over-weighted.
- **Early hold release needed no special-case branch**: judging the tail via the same `abs(error) → window bucket` logic used for taps naturally produces a Miss when released far before `end_time_ms`, since the error simply exceeds the Good window. One code path serves both "released on time" and "released too early."
- **Note-visual hide timing in `play_field.gd` is purely time-based**, not signal-correlated — simpler and just as correct, since a note's Good window elapsing means it has necessarily been resolved one way or another.

### Verification run (Godot 4.7.stable)
```
Ran 21 test(s): 21 passed, 0 failed.
```
`godot --headless --quit --path .` — clean. `godot --headless --path . res://game/play_field.tscn --quit-after 90` — runs ~1.5s of real gameplay (past the fixture's chord/tap windows) with no script errors. Manual in-editor F6 playtest (actually pressing the lane keys) still recommended before Stage 3 sign-off.

### What Stage 3 assumes
- `JudgeEngine`'s public state getters + signals (`judged`, `combo_changed`, `health_changed`, `song_failed`) are the HUD/results data surface — build the real HUD (2A) and results screen (§2.7, incl. `hit_errors` for the histogram) against these directly rather than re-deriving judgment state.
- `game/play_field.gd` is a disposable M1a proving ground (like Stage 1's `vertical_slice.gd`) — Stage 3 builds the real gameplay scene fresh around the same `JudgeEngine`/`Conductor`/`LaneInput` calls it validated, styled from the imported 2A mockup.
- Calibration screen (§2.8) writes to `SettingsStore.settings.audio_offset_ms/input_offset_ms`, which `Conductor` already reads — no engine-side changes needed, just the UI.

## Stage 3 — M1b: HUD, results, calibration, song select — first playable — ✅ COMPLETE (HUD import landed post-Stage-6)

### Gameplay HUD from imported mockup — DONE (post-Stage-6 follow-up session, correcting the record)
- [x] Gameplay HUD, variant **1a** ("Classic centered — compact playfield, top HUD row, horizontal health bar") — Godot Control nodes + one custom-draw layer, not HTML. **Correction: the original note here — that the Claude Design MCP wasn't connected — was wrong**, exactly the same mistaken conclusion Stage 6 already corrected for the editor (tested via `DesignSync.list_projects`, which only lists *design-system*-type projects; the right call is `DesignSync.get_project`/`list_files`/`get_file` against the **specific project ID**, `cc6f9e35-9183-4b42-8d8a-be6dfc135fe1`). `Octet - Gameplay HUD.dc.html` turned out to hold three layout directions (`1a`/`1b`/`1c`), same structure as the editor's file — **1a implemented**, matching the editor's 2a precedent. See `docs/DESIGN_HANDOFF.md` for the full correction and the two documented scope cuts (flat background instead of a breathing radial vignette; solid-colour health-bar fill instead of a gradient — both purely decorative, neither on `DESIGN_BRIEF.md`'s required HUD element list).
- **`game/playfield_view.gd`** (new, no `class_name` — pure display, referenced as plain `Control` from `gameplay.gd` exactly like `editor/waveform_view.gd`/`editor/note_timeline_view.gd` are from `editor_main.gd`): one custom-`_draw` layer for the whole 640×910 playfield (mockup-exact local coordinates) — lane separators, a glow-pulsing judgment line (same layered-line glow trick as the waveform view's playhead), key labels, tap/hold/chord notes as glowing circles, a hit-burst flash, and a rising/fading judgment-popup label. Fed per-frame state (`song_time_ms`, `scroll_speed`) and one-shot triggers (`trigger_hit_burst`, `trigger_judgment_popup`) from `gameplay.gd`; note position/visibility math (`judgment_y - (target_ms - song_ms) * PIXELS_PER_MS * scroll_speed`, time-based hide past the Good window) is unchanged from the pre-1a implementation.
- **`game/gameplay.tscn`/`.gd` rebuilt** around the 1a layout: top-left title/artist/difficulty-pill block, centred mono score, right-aligned mono accuracy/combo (with a brief scale-pulse on combo increases, skipped under reduced motion), a bordered horizontal health bar (`HealthBarTrack`/`HealthBarBackground`/`HealthBarFill`, width driven by `health_changed`), the `PlayfieldView`, a `REDUCED FLASH: ON/OFF` readout, and a `FailedLabel` overlay on `song_failed` (not in the mockup — an honest extension for a state the static mockup doesn't show, same precedent as Stage 6's editor utility bars). The old plain-Label HUD, the live grade label (grade belongs to Results only, per `DESIGN_BRIEF.md`/`PROJECT_BRIEF.md` §2.7 — dropped from the HUD to match the mockup and brief exactly), and the redundant lane-key hint label (superseded by `PlayfieldView`'s in-playfield key labels) are gone. `JudgeEngine` construction, `Conductor.play()` (incl. the playtest `pending_audio_stream` path), `LaneInput` press/release feeding, chart loading/fallback, chart-end/backing-track logic, and the finish→results routing are all unchanged.
- **First real use of `assets/fonts/font_mono.tres`/`font_display.tres`** project-wide (previously only `font_ui.tres` was wired into `octet_theme.tres`) — HUD numerics and overlines use mono (tabular, per `DESIGN_BRIEF.md` §3), the title and judgment popups use display. Still `SystemFont` fallbacks, not real font binaries (see `assets/fonts/README.md`, unchanged).
- **`game/judge_engine.gd`'s `judged` signal gained one parameter**, `affects_combo: bool` — the same internal flag `_apply_judgment` already computed (true for a real discrete note event, false for hold-tick credit / truncated-remaining-tick entries). Verified project-wide that `game/gameplay.gd` was the *only* connection point before extending it — zero blast radius, purely additive, no scoring/health/combo math touched. Needed because the HUD now wires the popup/hit-burst to real hits; without this flag a held note would spam a "PERFECT" popup every `hold_tick_interval_ms` while held. `_refresh_hud()` (score/accuracy) still runs for every judgment including ticks; only the popup/burst are gated to `affects_combo == true`.

### Verification (post-Stage-6 HUD-import session)
`godot --headless --quit` — clean. `godot --headless -s tests/run_tests.gd` — 46/46 pass (no judgment-logic behaviour changed, so no new test cases needed — same reasoning Stage 3 originally used for its non-HUD UI-flow work). `godot --headless --path . res://game/gameplay.tscn --quit-after 90` and `res://editor/editor_main.tscn --quit-after 30` (regression check) — both run error-free; the "2 ObjectDB instances leaked" warning on the gameplay scene was confirmed pre-existing and unrelated (identical warning reproduces on `game/vertical_slice.tscn`, untouched by this change — an `AudioStreamWAV`/`AudioStreamPlaybackWAV` artifact of `Conductor.play()` under the headless dummy audio driver at forced-quit time). No `class_name` was added, so no editor-cache-regen step was needed. **Still outstanding, carried forward:** a manual in-editor pass with real audio/mouse (song select → play with real key presses → results), same as every other screen in this project — headless can't drive real input or real audio.

### What's built (everything else in Stage 3, verified)
- **`core/play_session.gd`** (new `PlaySession` autoload, registered after `LaneInput`/before `Conductor` in `project.godot`): the scene-to-scene handoff `change_scene_to_file` doesn't provide — `chart_list`/`chart_index` (song select's ordered picks, for Results' "Next"), `mods` (chosen at song select), and `last_engine` (the finished `JudgeEngine`, read by results). A RefCounted autoload field survives scene changes fine since autoloads persist.
- **`game/song_select.gd`/`.tscn`**: scans `res://tests/fixtures/*.oct` (bundled demo content) and `user://songs/*.oct` (the real per-machine convention, auto-created if missing — a stand-in for real chart installation until Stage 4/5's audio import and `.octet` bundle unpacking exist) via `OctIO.load_oct`, lists title/artist/mapper/difficulty/star rating, a No-Fail checkbox, and Play/Calibrate/Back. Matches `ui/main.gd`'s pre-existing `PLAY_SCENE` constant (`res://game/song_select.tscn`) exactly — no change needed there.
- **`game/gameplay.gd`/`.tscn`** — supersedes Stage 2's `game/play_field.gd`/`.tscn` (which was explicitly documented there as disposable). Loads whichever chart `PlaySession` queued (falls back to the Stage 2 fixture when run standalone with nothing queued, so F6 still works during dev), builds the backing click track long enough to cover the chart's last note (`Metronome.build`, sized off the chart's own first timing point's BPM), and on completion (song time past the last note's Good window) hands the finished engine to `PlaySession.last_engine` and routes to results. Same Conductor/LaneInput/JudgeEngine wiring as `play_field.gd` — only the surrounding flow and chart-loading changed. Added a Quit button (`SceneRouter.go_back()`) as a safety valve, not specified by the brief but reasonable UX.
- **`game/results.gd`/`.tscn`** (§2.7): grade (big, amber), final accuracy, max combo, score, per-judgment breakdown, badges (Full combo / All Perfect / Unranked / FAILED), a hit-error histogram built from `JudgeEngine.hit_errors` (9 bars bucketed across the observed range — at least the Good window, wider if any early-hold-release outlier exceeded it — bottom-aligned via `SIZE_SHRINK_END` so they read as a growing-up bar chart), mean offset, and Retry/Next/Back. "Next" advances `PlaySession.chart_index` via `advance_to_next_chart()`. Online score submission (§2.7's "if online and ranked, submit score and show placement") is explicitly deferred — `Net.is_online()` is still Stage 7's unimplemented stub, so there's nothing to submit yet.
- **`audio/calibration.gd`/`.tscn`** (§2.8) — placed under `/audio` per PROJECT_BRIEF's own folder map ("`/audio` — conductor, calibration, analysis bindings"), not `/ui`. Tap-to-the-beat routine: plays a metronome (`Metronome.build`), flashes a pulse indicator on each beat, records the signed error of each tap (Godot's built-in `ui_accept` action — Space/Enter — reused rather than adding a dedicated InputMap action) after a warmup period, averages the error, and stores `-average_error` into `SettingsStore.settings.input_offset_ms` (sign flipped per the additive "positive = shift later" convention `Conductor` already uses — see decision note below). `audio_offset_ms` is deliberately left untouched.
- **Promoted `game/fixture_click_track.gd` → `audio/metronome.gd`** (`FixtureClickTrack` → `Metronome`): its click-track generator stopped being test-only the moment the calibration screen needed a real metronome to tap along to, so it moved out of `/game` test-fixture territory into `/audio` alongside `Conductor` as a first-class production utility. Updated its one other caller, `game/vertical_slice.gd`. No behavioural change, added `beat_interval_ms()`.

### Gotchas found during integration
1. **GDScript's static type checker can't infer the element type of a `for` loop over an untyped array literal** (`for x in [a, b]:` leaves `x` typed `Variant` even when `a`/`b` are both `String` constants), which then poisons any `:=` assignment built from it ("Cannot infer the type... because the value doesn't have a set type"). Fix: assign the literal to an explicitly typed `Array[String]` variable first, then loop over that. Hit in `game/song_select.gd`'s directory-scan loop.
2. **Headless `--headless -s`/`--quit` runs use the dummy audio driver, and playback position does not advance meaningfully under it** (per the Stage 0 handoff note) — so a live gameplay scene never naturally reaches its "song finished" transition in a headless smoke test. To verify `game/results.gd`'s populated code path (histogram/badges/breakdown) without a real audio device, a throwaway `SceneTree` script built a `JudgeEngine` directly, set `PlaySession.last_engine`, instantiated `results.tscn` manually, and checked for runtime errors — then was deleted. Worth remembering as the pattern for verifying any scene that depends on a finished play session, until real audio output is available to test against.
3. Same global-script-class-cache gotcha as Stages 1-2 applied again for `Metronome` after the rename.

### Design decisions worth carrying to the vault (per CLAUDE.md §1/§3)
- **Calibration cannot separate audio-latency bias from input-latency bias from a single tap-to-beat test** — they're confounded into one measured number. This screen attributes the whole measured average to `input_offset_ms` only, leaving `audio_offset_ms` at 0/user-set. If a future stage wants to disambiguate the two, it'll need a second, different routine (e.g. a known-latency audio loopback test) — this one routine can't do it alone.
- **`game/gameplay.gd`'s chart-end detection and backing-track sizing derive entirely from the chart's own data** (last note's time, first timing point's BPM) rather than a fixed duration, so it works for any `.oct`, not just the Stage 2 fixture — this is the seam Stage 4's real audio import should slot into (swap the `Metronome.build()` call for loading `chart.audio.filename` when it exists next to the chart).

### Verification run (Godot 4.7.stable)
```
Ran 21 test(s): 21 passed, 0 failed.
```
(No new automated tests added this stage — Stage 3's non-HUD pieces are UI flow, not new judgment logic, so they were verified via headless scene smoke tests instead: `godot --headless --quit` clean boot; `godot --headless --path . res://game/song_select.tscn --quit-after 30`, `res://game/gameplay.tscn --quit-after 90`, `res://audio/calibration.tscn --quit-after 30` all run error-free; `results.tscn`'s populated path verified via the throwaway script described above.) Manual in-editor playthrough (song select → gameplay → results, and the calibration routine with real taps) still recommended — headless dummy audio couldn't exercise real timing end-to-end.

### What's still needed before Stage 3 can be marked fully complete
- ~~Import HUD variant 2A~~ — done, see the HUD section above (turned out to be variant **1a**, not "2A").
- A manual in-editor playthrough with real audio timing (song select → play → results; calibration with real taps) to confirm feel, not just absence of script errors.

## Stage 4 — M2a: Editor shell, audio import, waveform, timing — ✅ COMPLETE

### Editor shell from imported mockup — DONE in Stage 6 (correcting the record)
- [x] Editor shell from the imported mockup (`Octet - Editor.dc.html`, option **2a** — not "1A", see `docs/DESIGN_HANDOFF.md`'s correction). The note originally here claimed the Claude Design MCP wasn't connected — **that was wrong** (see Stage 6's handoff for the full correction: `DesignSync.get_project`/`list_files`/`get_file` against the specific project ID works fine; only `list_projects`, scoped to design-system projects, came back empty). `editor/editor_main.tscn` was rebuilt in Stage 6 around the real 2a layout. The functional bullets below (audio import/waveform/timing/transport) all still stand as originally shipped in this stage — only the *layout* changed in Stage 6.

### What's built (everything else in Stage 4, verified)
- **`editor/audio_import.gd`** (`AudioImport`) — accepts MP3/OGG Vorbis/WAV (§3.1) via `AudioStreamWAV/MP3/OggVorbis.load_from_file()` (all three confirmed to exist in Godot 4.7 — verified empirically before committing to this API, see gotcha below). `build_waveform_peaks(stream, bucket_count)` decodes the **actual audio** offline into a peak-amplitude envelope for the waveform — no realtime audio device needed — using `AudioStreamPlayback.mix_audio(rate_scale, frames)`, a real scriptable method on every playback subclass that returns genuine decoded PCM frames and signals end-of-stream by returning fewer frames than requested. Verified against a `Metronome`-built click track: peaks landed exactly on the known click positions (0/500/1000/1500ms) with silence everywhere else.
- **`editor/beat_grid.gd`** (`BeatGrid`) — multi-timing-point beat math (§3.4): `beat_times_ms()` walks each timing point's segment generating beat boundaries at its own BPM (the next timing point's `time_ms` ends the segment); `active_timing_point()` finds which timing point governs a given time; `snap_time_ms()` snaps to the nearest grid line at a given division under the active timing point. Also exposes `SNAP_DIVISIONS` (1/1 through 1/16) ready for Stage 5's note placement to consume — the selector UI itself isn't built yet since it's tied to note placement, out of Stage 4's scope.
- **`editor/waveform_view.gd`** — a custom-`_draw()` `Control` rendering the peak envelope as bars (upgraded to a polyline in Stage 6 to match the imported mockup), beat lines from `BeatGrid`, and a playhead (§3.3); `x_to_time_ms()` converts a click position back to song time for scrubbing.
- **`editor/editor_main.gd`/`.tscn`** — the functional shell (layout rebuilt in Stage 6 around the imported 2a mockup; the controls/logic below shipped here first and carried forward unchanged): Import button + `FileDialog` (real runtime file picker, not editor-only), BPM/offset `SpinBox`es bound to the first timing point, an "add timing point at playhead" button with per-row remove (guarded so the first timing point — the song's offset — can't be removed), Play/Pause/Stop, a 0.25×/0.5×/0.75×/1× rate selector, and click-to-seek on the waveform. Reuses `Conductor` for all playback/scrub (added `set_playback_rate()`/`playback_rate()` there, backed by `AudioStreamPlayer.pitch_scale` — gameplay never calls it, so no regression risk to Stages 1-3). No save/export yet (`Chart`/`TimingPoint` state lives in memory only) — that's Stage 5.
- **Tests**: `tests/test_beat_grid.gd` (`TestBeatGrid`, 4 cases) and `tests/test_audio_import.gd` (`TestAudioImport`, 3 cases, including the WAV round-trip via `save_to_wav()`/`load_audio_file()` and the click-position waveform check), registered in `run_tests.gd`.

### Gotchas found during integration
1. **Godot 4.7's offline audio-decode API isn't obvious from memory alone, so it was verified empirically before writing `audio_import.gd` against it** (see the pattern: build a synthetic WAV via `Metronome`, `save_to_wav()` it to a real file, then probe the API with throwaway scripts) — confirmed: `AudioStreamWAV/MP3/OggVorbis.load_from_file(path)` all exist and accept `user://` paths directly (not just OS-absolute paths); `AudioStreamWAV.save_to_wav(path)` exists; and critically, `AudioStreamPlayback.mix_audio(rate_scale, frames) -> PackedVector2Array` is a real, scriptable, non-underscore-prefixed method present on every playback subclass that decodes actual PCM with no realtime audio device involved — this is what makes offline waveform generation (and, later, Stage 6's onset/BPM analysis) possible in pure GDScript at all.
2. **No format-agnostic way to query an `AudioStream`'s sample rate from GDScript** — `AudioStreamWAV` exposes `.mix_rate`, but `AudioStreamMP3`/`AudioStreamOggVorbis` expose neither a `mix_rate` property nor any `get_stream_sampling_rate()`-style method (checked via `ClassDB.class_get_property_list`/`class_get_method_list` — confirmed absent, not just undocumented). Worked around by bucketing the waveform envelope by **actual decoded frame count** (a full decode pass first, bucket afterward) instead of a precomputed `duration_sec * sample_rate` total. Worth remembering for Stage 6's DSP work, which will hit the same gap.
3. GDScript's static type checker can't infer the element type of a `for` loop over an untyped array literal (same gotcha as Stage 3's `song_select.gd` — didn't recur here since `beat_grid.gd`/`audio_import.gd` were written with this already in mind, but worth restating since it'll keep coming up).

### Design decisions worth carrying to the vault (per CLAUDE.md §1/§3)
- **The offline `mix_audio()` decode approach is the seam Stage 6's real onset-detection/BPM-estimation DSP should build on** — it already gives genuine PCM frames without a Rust GDExtension. Whether pure-GDScript FFT/spectral-flux performance on a 3-5 minute song is "tolerable" (PROJECT_BRIEF §3.2's stated MVP-fallback bar) is the open question Stage 6 needs to measure before deciding GDScript vs. the Rust GDExtension path — this stage's work at least de-risks the "can we get PCM at all" half of that decision.
- **Editor transport reuses the same `Conductor` autoload as gameplay** rather than a separate playback path, consistent with CLAUDE.md's "Conductor clock is central" rule — variable rate is a pitch_scale change (speed+pitch together, no time-stretch DSP), an honest, simple choice rather than a more complex pitch-preserving stretch.

### Verification run (Godot 4.7.stable)
```
Ran 28 test(s): 28 passed, 0 failed.
```
`godot --headless --quit` clean boot. `godot --headless --path . res://editor/editor_main.tscn --quit-after 30` runs error-free. Full import → waveform decode → BPM/offset edit → add/remove timing point → play/stop flow verified end-to-end via a throwaway script driving `editor_main.gd`'s handlers directly with a real saved WAV file (bypassing the `FileDialog` UI, which needs a real click) — no runtime errors. Manual in-editor test (actually clicking Import and picking a real MP3/OGG/WAV file, dragging the waveform to scrub, changing rate) still recommended — not yet done with a real audio file or real user interaction.

### Stage 4 sign-off
Editor shell import landed in Stage 6 (see above and Stage 6's own section). Still outstanding, carried forward: a manual in-editor test with a real audio file (the throwaway verification only used a synthetic `Metronome` WAV) and real mouse/keyboard interaction.

## Stage 5 — M2b: Note editing, QOL, save/export, playtest — ✅ COMPLETE

Every checklist item's logic and interaction model was built and verified here. The note-tool palette (Tap/Hold/Select modes, replacing the modifier-key stand-in mentioned below) was properly built in Stage 6 once the real mockup was importable — see that section.

### What's built
- **`editor/note_editor.gd`** (`NoteEditor`) — pure, in-place note operations over `Array[ChartNote]`: `place_tap`/`place_hold` (replaces any existing note at the same lane+time rather than stacking; hold end clamped to at least 1ms past start), `remove_note_at`, `delete_notes` (by reference), `mirror_notes` (`lane = 7 - lane`), `nudge_notes` (shifts head+tail by a delta), `duplicate_notes` (deep-copies with a time offset — the shared building block for both "Duplicate" and "Paste"), `notes_in_range` (the box-select query). Deliberately separated from both UI input and undo/redo so it's fully headless-testable (`tests/test_note_editor.gd`, 7 cases).
- **`editor/undo_stack.gd`** (`EditorUndoStack`) — snapshot-based undo/redo (§3.6 "full history"): `record(notes)` deep-copies the *pre*-mutation state before an edit; `undo`/`redo` swap between undo/redo stacks, returning the array to install. Chosen over a command-object pattern deliberately — simpler and less bug-prone at chart-sized note counts, at a negligible memory cost. 3 tests (`tests/test_undo_stack.gd`) confirm the round-trip and that a fresh `record()` after an undo correctly invalidates the stale redo branch.
- **`editor/note_timeline_view.gd`** — the note timeline (§3.5): eight lanes rendered as stacked horizontal rows sharing the waveform's x-axis-as-time convention (a deliberate simplification over a scrolling vertical piano-roll — see design decision below, still true after Stage 6). Click places a tap; click-drag places a hold; Shift+drag box-selects instead (a modifier-key stand-in for a real note-tool palette — **replaced in Stage 6** with explicit Tap/Hold/Select tool modes once the real mockup was importable); right-click deletes. Emits signals only — `editor_main.gd` owns all mutation/undo-recording, matching `waveform_view.gd`'s pure-display precedent.
- **`editor/editor_session.gd`** (new `EditorSession` autoload, registered after `PlaySession`/before `Conductor`) — single source of truth for the open project (audio, shared timing points, one `Chart` per difficulty tab) so a **playtest-in-editor** round trip survives `SceneRouter`/`change_scene_to_file` destroying and recreating the scene instance. Also the autosave/crash-recovery store: `autosave()` writes one `.oct` per difficulty plus a small `meta.json` to `user://autosave/`; `load_autosave()` restores it (audio itself is re-linked by path, not silently re-decoded, since decode is a visible, potentially-slow operation better left explicit).
- **`editor/editor_main.gd`/`.tscn`** rewritten to sit entirely on top of `EditorSession` (no more locally-duplicated chart/stream state): difficulty tabs (`TabBar`, add/remove, each sharing audio/timing but with independent notes/metadata per §3.6), snap-division selector (finally wired now that note placement exists, using `BeatGrid.SNAP_DIVISIONS` from Stage 4), time + row zoom sliders (scale the waveform/timeline `custom_minimum_size` inside `ScrollContainer`s, which are kept horizontally in sync every frame), a metronome toggle (plays `Metronome.build_single_click()` on each detected beat crossing during playback), select/copy/paste/duplicate/mirror/delete (Ctrl+C/V, arrow-key nudge, buttons), undo/redo (Ctrl+Z/Y + buttons), "Save chart (.oct)", "Export bundle (.octet)", and "Playtest" (hands `EditorSession.active_chart()` + `EditorSession.audio_stream` to `PlaySession.pending_chart`/`pending_audio_stream`, then `goto_scene_pushed` into `game/gameplay.tscn`).
- **`core/play_session.gd`** gained `pending_chart`/`pending_audio_stream` (+ `take_*` consumers) — the playtest bridge. **`game/gameplay.gd`** now checks these first before falling back to its normal load-from-`.oct`-path / `Metronome`-backing-track flow, so playtesting plays the *actual* imported audio, not a click track.
- **`core/octet_bundle.gd`'s `write_bundle` completed** (§4.2, was a Stage 0 manifest-only stub): packs the real audio file (`song.<ext>`), one `chart_<difficulty>.oct` per difficulty (via a new public `OctIO.chart_to_json()`, exposing what `save_oct` already built internally — no temp files needed), and a `manifest.json` merging the caller's dict with the computed audio filename, per-difficulty list, and a SHA-256 checksum of the packed audio (`HashingContext`, verified against a known hash before trusting it). Cover/background art isn't wired into the editor's export flow yet (no art picker built) but `write_bundle` accepts it via the manifest dict if a future caller adds it.
- **`audio/metronome.gd`** gained `build_single_click()` (shares its click-rendering math with `build()` via a new private `_render_click()` helper) for the editor's metronome toggle.
- **Tests**: `tests/test_note_editor.gd` (7), `tests/test_undo_stack.gd` (3), `tests/test_octet_bundle.gd` (2, full write→read_manifest and write→raw-zip-entry round trips), registered in `run_tests.gd`. 40 total, all passing.

### Gotchas found during integration
1. **The `-s` entry-point script itself — not just `class_name`-declared scripts — can't statically reference an autoload identifier.** Stage 0's original finding named `class_name` scripts specifically; a throwaway verification script here (plain `extends SceneTree`, no `class_name`) failed the same way (`Compile Error: Identifier not found: EditorSession`) the moment it referenced the autoload directly. `tests/run_tests.gd` had already avoided this by convention (always going through `get_autoload()`), which in hindsight was protecting the entry script itself, not only the suites it loads. Refined understanding: **any script that will ever be the direct `-s` target must avoid static autoload references**, full stop — use `Engine.get_main_loop().root.get_node("Name")` instead. Doesn't affect normal scene scripts loaded through the regular boot path (`editor_main.gd` references `EditorSession` statically throughout and works fine both via `--headless --quit` and in a running scene).
2. **`TabBar` auto-selects tab 0 the moment it goes from empty to non-empty, firing `tab_changed(0)` mid-loop** while `_rebuild_difficulty_tabs()` was still calling `add_tab()` for each difficulty — this overwrote `EditorSession.active_difficulty_index` (set to the *intended* new tab just before the rebuild) back to 0 before the function's own explicit `current_tab = target_index` assignment ran, because that assignment read the now-corrupted value. Caught by the end-to-end verification script (added a difficulty, expected `active_difficulty_index == 1`, got `0`). Fixed by capturing the target index in a local *before* touching the `TabBar` at all, then re-asserting it on `EditorSession` after the rebuild completes — robust regardless of exactly when `TabBar` decides to auto-select. Worth remembering for any other Godot control that "helpfully" auto-selects/auto-fires signals as items are added.
3. Verified `ZIPPacker`/`ZIPReader`/`FileAccess` all accept `user://` paths directly (no `ProjectSettings.globalize_path()` needed) before writing `OctetBundle.write_bundle` and its tests against that assumption.

### Design decisions worth carrying to the vault (per CLAUDE.md §1/§3)
- **The note timeline is eight stacked horizontal rows sharing the waveform's time axis, not a scrolling vertical piano-roll matching the gameplay fall direction.** PROJECT_BRIEF §3.5 literally says "a vertical lane timeline" — this is a deliberate simplification, chosen because it keeps the interaction model (click/drag, all working in one shared time-axis coordinate space with the waveform above) simple enough to build and headless-verify. Still true after Stage 6's mockup import — the underlying signals (`tap_place_requested` etc.) and `NoteEditor` operations didn't need to change regardless of how the timeline is visually arranged.
- **Modifier keys (Shift+drag for box-select) stood in for a dedicated note-tool palette (tap/hold/select) until Stage 6**, once the real mockup was importable and an explicit Tap/Hold/Select tool-mode rail was built instead.
- **Undo/redo is snapshot-based, not command-object-based.** Chart-sized note counts make whole-array deep-copies cheap; this trades a small, constant memory cost for eliminating an entire category of bugs (mismatched do/undo closures, partial-application edge cases) that a command pattern would risk.

### Verification run (Godot 4.7.stable)
```
Ran 40 test(s): 40 passed, 0 failed.
```
`godot --headless --quit` clean boot. `godot --headless --path . res://editor/editor_main.tscn --quit-after 60` and `res://game/gameplay.tscn --quit-after 60` (regression-checking the playtest-bridge changes) both run error-free. Full end-to-end flow — import audio, place a chord + hold, undo/redo, box-select-all, mirror, duplicate, copy/paste, add/remove a difficulty tab, save chart, export bundle, playtest handoff (confirmed `PlaySession.pending_chart`/`pending_audio_stream` both set), autosave, reset, and reload-from-autosave (confirmed notes/audio path correctly restored) — verified via a throwaway script driving `editor_main.gd`'s handlers directly (bypassing `FileDialog`/mouse UI, and note timeline placement's own mouse handling, both of which still need a real manual pass). This is where the `TabBar` bug above was actually caught.

### Stage 5 sign-off
Editor shell + note-tool palette import landed in Stage 6 (see below) — none of Stage 5's underlying logic (`NoteEditor`, `EditorUndoStack`, `BeatGrid`, `AudioImport`, `EditorSession`, `OctetBundle`) needed to change. Still outstanding, carried forward: a manual in-editor pass with a real audio file and real mouse interaction (dragging out a hold, box-select, the metronome's audible click, playtest actually sounding like the imported song).

## Stage 6 — M3: Automatic analysis — ✅ COMPLETE
Plus, landed in the same session: the Editor shell import (previously reported blocked — that was wrong) and a button-hitbox bug fix.

### What's built: the DSP pipeline (§3.2)
- **`editor/dsp_fft.gd`** (`DspFft`) — a from-scratch radix-2 iterative Cooley-Tukey FFT. Godot exposes no FFT to GDScript (`AudioEffectSpectrumAnalyzer` is a real-time audio-bus effect, not usable for offline whole-buffer analysis), so this is hand-rolled: in-place `fft(re, im)` on parallel real/imaginary `PackedFloat32Array`s (bit-reversal permutation + butterfly stages with incrementally-rotated twiddle factors, avoiding a sin/cos call per butterfly), `magnitude_spectrum()`, and a cached `hann_window()`. **Verified correct in isolation first** (`tests/test_dsp_fft.gd`: a pure sine at a known bin peaks at that exact bin; a DC signal peaks at bin 0) before anything was built on top of it — same discipline as verifying `mix_audio` empirically in Stage 4.
- **`editor/audio_analysis.gd`** (`AudioAnalysis`), the brief's fixed interface: `analyze(pcm: PackedFloat32Array, sample_rate: float) -> {"bpm": float, "offset_ms": float, "onsets": Array[float]}`. Pipeline exactly per §3.2: 1024-sample windows at 50% overlap → Hann → FFT → spectral flux (positive magnitude deltas vs. the previous frame) → an onset envelope; adaptive-threshold (mean + k·stddev) peak-picking with a minimum inter-onset gap; autocorrelation over the 60-220 BPM lag range for tempo; octave-error resolution folding toward a 90-180 BPM "preferred" band; beat-phase/offset via a strength-weighted histogram of `onset_time mod beat_interval`.
- **No Rust toolchain is available in this environment** (`rustc`/`cargo`: command not found) — this settles §3.2's own "decide Rust GDExtension vs GDScript fallback" in favour of the GDScript path the brief explicitly allows as an MVP.
- **`editor/audio_import.gd` refactored**: extracted `decode_full_pcm(stream) -> PackedFloat32Array` (mono signed samples, one decode pass) and `effective_sample_rate(samples, duration_sec)` so a song is decoded **once** and shared between the waveform envelope and analysis, instead of decoding twice. `build_waveform_peaks()` now takes pre-decoded samples rather than a stream.
- **`editor/editor_session.gd`** gained `pcm_samples`/`pcm_sample_rate`, populated once at import.
- **`editor/editor_main.gd`**: an "Analyze" action runs `AudioAnalysis.analyze()` on a real background `Thread` (polled each `_process()` via `is_alive()`, retrieved with `wait_to_finish()`) so a multi-second analysis doesn't freeze the UI — the PCM/sample-rate are captured as local copies and `.bind()`-ed into the callable before the thread starts, so the background thread never touches the `EditorSession` autoload at all (`PackedFloat32Array`'s copy-on-write semantics make this a cheap, race-free snapshot). Results are applied as **editable** values (per §3.2) into the normal BPM/offset `SpinBox`es; onset-aligned taps are seeded onto the active chart **only if it currently has zero notes** — a non-destructive default that never silently clobbers existing work.
- **Tests**: `tests/test_dsp_fft.gd` (3 cases), `tests/test_audio_analysis.gd` (2 cases: a known-BPM `Metronome` fixture with tolerance, and a timed performance benchmark).

### Real bug caught and fixed during verification: autocorrelation octave-locking
The first BPM-tolerance test run failed outright: a 128 BPM fixture resolved to **63.8 BPM** (a clean octave error) even though the onset detector itself found onsets at the correct spacing. Diagnosed with a throwaway script dumping per-lag autocorrelation scores rather than guessing. Two real bugs, found in sequence:
1. `_autocorrelation_at_lag` divided by the shrinking overlap count (`envelope.size() - lag`), computing a *mean* instead of the textbook autocorrelation *sum* — this systematically inflates scores at larger lags, biasing toward octave-doubling. Fixed by returning the raw sum.
2. Even after that fix, resolution still failed: the raw best lag (81, ≈64 BPM) was genuinely double the true period (40, ≈129 BPM) in this fixture — a real effect of the true beat interval not being an integer number of frames, causing detected onset spacing to alternate between two adjacent frame counts (jitter that a lag spanning *two* beats absorbs more consistently than either single-beat lag alone). The octave-fold logic checked candidates via `round(best_lag / 2.0)` — a single rounded value — but `best_lag` was **odd** (81), so `81/2.0 = 40.5` rounds to 41, which scored *below* the fold threshold, while lag 40 (the one that actually worked) was never checked. Fixed by checking **both** `floor(best_lag/2.0)` and `ceil(best_lag/2.0)` as candidates and picking whichever scores best, not a single rounded guess.

Both fixes are documented inline in `editor/audio_analysis.gd` with the exact numbers that exposed them. Worth remembering generally: **when an average-of-N normalization uses an N that varies with the thing you're comparing across, it silently biases the comparison** — this is a classic, easy-to-miss autocorrelation pitfall, not specific to this codebase.

### The open performance question, answered
A 30-second synthetic click track analyzes in ~3.2-3.7 seconds (measured across several runs); linear extrapolation (the pipeline's cost scales linearly with sample count — windowed FFT+flux per frame, and autocorrelation over a *fixed* lag range regardless of song length) puts a full 4-minute song at **~25-30 seconds**. That's the concrete answer to the question Stage 4's handoff left open ("is pure-GDScript FFT/spectral-flux performance tolerable?") — yes, comfortably, for a one-time import-time operation, especially since it now runs on a background `Thread` and doesn't block the editor UI.

### What's built: Editor shell import + button-hitbox fix
- **The Claude Design MCP access was previously (incorrectly) reported as unavailable across Stages 3-5.** Re-investigated at the user's request: `DesignSync.get_project`/`list_files`/`get_file` against the **specific project ID** (`cc6f9e35-9183-4b42-8d8a-be6dfc135fe1`) works fine and pulled the real `Octet - Editor.dc.html` + `Octet - Components.dc.html` content directly. The earlier conclusion was based only on `DesignSync.list_projects`, which is scoped to *design-system*-type projects and correctly returns empty for a project like this one (confirmed `PROJECT_TYPE_PROJECT` via `get_project`, not `PROJECT_TYPE_DESIGN_SYSTEM`). See `docs/DESIGN_HANDOFF.md`'s correction note.
- **`Octet - Editor.dc.html` contains three layout options** (internally labelled `2a`/`2b`/`2c`, not the "1A" shorthand `DESIGN_HANDOFF.md` had guessed before ever fetching the file — it had explicitly flagged that as unconfirmed). **Implemented option 2a** ("Standard DAW layout — waveform top, tool rail left, inspector right"), the file's own suggested next step.
- **`editor/editor_main.tscn`/`.gd` fully rebuilt** around 2a's structure — top bar (wordmark/transport/rate/metronome) → difficulty tabs + BPM/offset chips + a snap segmented control (real toggle buttons, one per `BeatGrid.SNAP_DIVISIONS`, replacing the Stage 4 `OptionButton`) → waveform panel → body (tool rail / note timeline / inspector) → two bottom utility bars (Undo/Redo/Duplicate/Analyze/Save/Export/Playtest, then Zoom/Metronome/Status/Back). Every row is now a real auto-flowing `VBoxContainer`/`HBoxContainer`/`PanelContainer` — **no hand-computed pixel offsets anywhere in this scene anymore**, which is also the hitbox fix (below).
- **`editor/note_timeline_view.gd` gained real tool modes** (`Tool.TAP`/`HOLD`/`SELECT` enum, `set_tool_mode()`), replacing the Stage 5 Shift-modifier stand-in — this is the actual note-tool palette PROJECT_BRIEF §3.5 asked for, driven by a tool rail built procedurally in `editor_main.gd` (`ButtonGroup`-linked toggle buttons, styled pink-active/surface-inactive). Also added `set_free_place()` (§3.5's "free-place (snap off) for off-grid notes", not previously built) and a compact read-only Inspector panel showing the selected note's lane/type/time/beat-snap.
- **`editor/waveform_view.gd` upgraded** from discrete vertical bars to a smooth polyline through the peak envelope (closer to the mockup's continuous waveform look), plus a layered-line glow approximation on the playhead (Control 2D drawing has no native glow/shadow filter, so several progressively-wider, more-transparent lines behind a crisp center line fake it cheaply) and brighter-every-4th-beat grid lines matching the mockup.

### Button-hitbox bug: root cause and fix
Investigated via a full audit of every `.tscn` scene under `ui/`, `game/`, `audio/`, `editor/` (dispatched to a subagent given the breadth). **Root cause fully isolated to the pre-Stage-6 `editor/editor_main.tscn`**: five control rows (TransportRow, TimingRow, DifficultyRow, EditActionsRow, ExportRow) plus a bare `BackButton` were built with `layout_mode = 0` and hand-computed pixel offsets, every row pinned to exactly **36px**, while the theme's `Button` minimum height (`content_margin_top/bottom = 8.0` + `default_font_size = 16`'s line height) is itself ~35-37px — and taller still for `OptionButton`/`SpinBox`/`TabBar`. Children overflowed their declared 36px rows into the 8-12px gaps between rows, so a control's real clickable `Rect2` extended into where the *next* row's control was visually drawn. **Every other scene** (`main.tscn`, `results.tscn`, `calibration.tscn`, `rebind_panel.tscn`, `song_select.tscn`, `gameplay.tscn`) already used container-managed (`layout_mode = 2`) auto-sizing and was confirmed **not** affected. Since `editor_main.tscn` was being fully rebuilt anyway (above), the fix was inherent in that rebuild — no more hand-placed fixed-height rows, so this class of bug is now structurally impossible in that scene.

### Gotchas found during integration
1. **Autocorrelation normalization bias** — see the "real bug caught" section above; the fuller technical detail is inline in `editor/audio_analysis.gd`.
2. **A frame-count-based wait loop doesn't map to real time in headless mode.** A verification script polling `_analysis_pending` for up to 300 `await process_frame` iterations exited immediately having "waited" 0 or 300 frames without the analysis actually finishing — headless ticks frames far faster than real time when there's nothing to render, so a frame budget doesn't give a background `Thread` genuine wall-clock CPU time. Fixed by polling with `Time.get_ticks_msec()` elapsed-time budget plus a real `OS.delay_msec()` in the wait loop instead of a frame-count budget. Worth remembering for any future test that waits on a background `Thread` from a headless `-s` script.
3. **`TabBar.add_tab()`'s auto-select-tab-0 behaviour (documented in Stage 5) recurred identically** in the rebuilt `editor_main.gd` — the existing `_rebuild_difficulty_tabs()` fix (capture the target index before touching `TabBar`, re-assert after) carried over unchanged and still works.
4. Verified `HashingContext` (SHA-256, used by `OctetBundle.write_bundle` since Stage 5) and `TabBar.clear_tabs()`/`add_tab()` behaviour empirically before relying on them, continuing the project's established "verify unfamiliar Godot API with a throwaway script before trusting it" discipline.

### Design decisions worth carrying to the vault (per CLAUDE.md §1/§3)
- **The offline `mix_audio()`-based decode approach Stage 4 built genuinely scales to real analysis work** — no native extension was needed for either PCM decode or the full FFT/autocorrelation pipeline, and performance is comfortably tolerable (~25-30s for a 4-minute song, one-time, backgrounded). This resolves the Rust-vs-GDScript question for good, not just provisionally — there's no remaining reason to revisit a Rust GDExtension for this unless a future need (e.g. wanting sub-second analysis, or reusing the DSP for something latency-sensitive) specifically demands it.
- **Two Design-MCP corrections are now on record** (here and in `docs/DESIGN_HANDOFF.md`) specifically so a future stage doesn't re-conclude "blocked" from an incomplete check: always try `get_project`/`list_files` against the *specific* project ID before concluding a Claude Design project is inaccessible — `list_projects` alone only surveys design-system-type projects.
- **Not every functionally-necessary control has an obvious home in a single static mockup frame** — mockups show one moment, not every settings/utility state. The bottom utility bars (Undo/Redo/Save/Export/Playtest/Analyze/Zoom/Status) are an honest extension beyond 2a's literal pixels, styled consistently with its visual language, not a deviation from it.

### Verification run (Godot 4.7.stable)
```
Ran 46 test(s): 46 passed, 0 failed.
```
`godot --headless --quit` clean boot. `godot --headless --path . res://editor/editor_main.tscn --quit-after 60`, `res://game/gameplay.tscn --quit-after 60`, and `res://game/vertical_slice.tscn --quit-after 30` (regression checks) all run error-free. Full end-to-end flow verified via a throwaway script: import a synthetic `Metronome` fixture → Analyze (waited on the real background thread, confirmed ~129 BPM / ~51ms offset / 63 seeded onset notes, consistent between the isolated unit test and the full editor flow) → manual tap/hold placement via the timeline-view signal handlers → undo/redo → box-select → add/remove difficulty tab → save chart → export bundle → playtest handoff (confirmed both `PlaySession.pending_chart`/`pending_audio_stream` set) — no runtime errors anywhere. Manual in-editor pass with real mouse interaction (dragging tool-rail buttons, actually clicking Analyze, watching the metronome/playhead glow) still recommended, as with every prior stage — headless can't drive real mouse/audio.

### What's still needed
- A manual in-editor pass with a real audio file and real mouse interaction — same outstanding item carried forward from every prior editor stage.
- ~~The Gameplay HUD import~~ — done in a post-Stage-6 follow-up session, see the Stage 3 HUD section above (variant **1a**, not "2A").
- Cover/background art isn't wired into the editor's export flow (no art picker built yet) — `OctetBundle.write_bundle` already accepts it via the manifest dict if a future caller adds it.

## Post-Stage-6 fidelity pass — global hitbox fix + full-game mockup rebuild — ✅ COMPLETE

Triggered by two user-reported bugs during manual playthrough: every button's clickable hitbox sat lower than its visual, and gameplay didn't match the Claude design mockup. Full details and the list of documented deviations are in `docs/DESIGN_HANDOFF.md`'s "Fidelity pass" section — summary here for the stage log.

### What's built
- **`project.godot` `[display]`**: added `window/size/window_width_override=1280`/`window_height_override=720` (same 16:9 as the 1920×1080 design canvas). Root cause was the window opening at the full 1920×1080 with no size override, so on any display where that doesn't fit uncropped the OS-resized window and the render canvas ended up scaled/shifted relative to the input transform — a uniform offset on every Control in every scene. Distinct from Stage 6's editor-only fixed-row-height hitbox fix (per-scene layout, not window config).
- **New shared component `ui/radial_background.gd`/`.tscn`**: reusable radial top-glow + breathing pink vignette background (`GradientTexture2D`-based, reduced-motion aware), instanced by Main Menu, Gameplay HUD, Results, and Calibration — all four mockups share this exact treatment.
- **`ui/main.tscn`/`.gd` rebuilt** to match `Octet - Main Menu.dc.html` exactly: animated 8-bar tick-mark logo (staggered pulse), glowing wordmark, description, a 4-card menu (Play filled pink / Editor·Browse maps·Profile outlined, each with a key hint), a top-right profile chip (honest "Guest/OFFLINE" placeholder, not the mockup's sample data), drifting ambient lane-colour columns on the right, and a bottom version/Settings bar. Supersedes the Stage 0 centered placeholder.
- **`game/song_select.tscn`/`.gd` rebuilt** to match `Octet - Song Select.dc.html`: two-pane layout — a left list pane (search field, Difficulty/Recent/A–Z sort, scrollable rows with a cover-art placeholder swatch, star badge, duration, selected-row highlight) and a right detail pane (preview box, title/artist/BPM/length, difficulty chips, a "YOUR BEST" card, Play). Same-song `.oct` files (matched by title+artist) are now grouped into difficulty variants for the chip UI — a new, lightweight grouping layer over the unchanged flat file scan; `OctIO`/`PlaySession`/No-Fail/Calibrate wiring is unchanged.
- **Gameplay HUD fidelity gaps closed** (`game/gameplay.tscn`, `game/playfield_view.gd`, `game/gameplay.gd`): restored the radial background + breathing vignette and the health bar's rounded pink→amber gradient fill (both previously cut as "decorative" — see the corrected record in `docs/DESIGN_HANDOFF.md`); hit bursts now use the struck lane's colour (`DesignTokens.lane_color`) instead of a hardcoded amber, per `DESIGN_BRIEF.md` §5; key-label y corrected to the mockup's actual 788 (was 800).
- **`game/results.tscn`/`.gd` rebuilt** to match `Octet - Results.dc.html`: a large glowing grade, a stats row (score/accuracy/max combo), a per-judgment breakdown (proportional bars per tier), and a hit-timing histogram card with bars coloured on an orchid→pink→coral gradient by bucket position (matching the mockup's SVG, not by count magnitude as the old version did). Same `PlaySession.last_engine` data surface as before; verified via the same "throwaway `SceneTree` script" pattern Stage 3 established for exercising the populated-results path without real audio.
- **`audio/calibration.tscn`/`.gd` rebuilt** to match `Octet - Calibration.dc.html`: a circular metronome visual (concentric rings, a real beat-synced pulse on the top tick + centre glow — driven by the actual metronome beat, not the mockup's fixed-period CSS loop, which reads truer than mimicking an animation unrelated to the real BPM), a beat counter + 8 progress pips, and three offset cards (AUDIO/INPUT/TOTAL). The tap-to-beat routine and the `-average_error → input_offset_ms` write are unchanged; AUDIO/TOTAL cards are shown honestly against the existing confounded-measurement design decision (AUDIO reflects whatever `SettingsStore.settings.audio_offset_ms` already holds — this routine still never writes it — TOTAL is the real sum Conductor applies).
- **`editor/editor_main.gd` residual fix**: re-audited 2a against a freshly re-fetched `Octet - Editor.dc.html`; the top bar's Play/Stop transport buttons now use the same 34px rounded icon-square treatment (`_tool_style()`) as the tool rail, matching the mockup — the rest of 2a's structure (top bar, difficulty bar, waveform, tool rail/timeline/inspector, bottom action/utility bars) was already a genuine mockup-driven build from Stage 6 and didn't need a rebuild.
- **New standing rule** (`CLAUDE.md` §2): all UI/visual work must be checked against the mockup re-fetched live from the Claude Design MCP, never built from memory/prose/baked constants alone — this is the rule that would have prevented the drift this pass fixed.

### Verification run (Godot 4.7.stable)
```
Ran 46 test(s): 46 passed, 0 failed.
```
`godot --headless --quit` clean boot. `godot --headless --path . <scene> --quit-after 20/30` for every rebuilt scene (`ui/main.tscn`, `game/song_select.tscn`, `game/gameplay.tscn`, `game/results.tscn`, `audio/calibration.tscn`, `editor/editor_main.tscn`) plus a regression check on the untouched `game/vertical_slice.tscn` — all run error-free (the "2 ObjectDB instances leaked" warning on the audio-driving scenes is the same pre-existing, confirmed-unrelated headless-audio artifact noted since Stage 3, reproducing identically on the untouched vertical-slice regression check). Results' populated path (breakdown bars, histogram) verified via a throwaway `SceneTree` script (deleted after use), same pattern as Stage 3's handoff. **Still outstanding, carried forward from every prior stage:** a manual in-editor pass with real mouse/keyboard input across every screen — headless can't drive real input, and this is the first time to actually confirm the hitbox fix and the visual fidelity by eye.

### What's still needed
- The manual in-editor pass above — this is the check that closes the loop on both bugs this pass fixed.
- Sign In / Map Hub / Profile screens still need building from their own mockups when Stage 7/8 land (now covered by the `CLAUDE.md` design-fidelity rule).

## Stage 7 — M4: Online core — ⬜ not started
- [ ] `/net` REST client (Firebase Auth/Firestore/Storage/Functions via `HTTPRequest`).
- [ ] Auth (email/password + Google). Publish/upload. Map hub browse/search/download. Per-map leaderboards. Score submission + server-side validation Cloud Function.
- Subagents: (1) `/net` + Auth, (2) Publish + hub, (3) Leaderboards + Cloud Functions.
- Done when: sign in → upload → hub → download → play → submit score → see on leaderboard, against a dev Firebase project.

## Stage 8 — M5: Community, polish, packaging — ⬜ not started
- [ ] Global ranks/performance-points (scheduled Cloud Function). Profiles. Ratings. Anti-cheat groundwork (replay capture scaffolding).
- [ ] Star-rating formula finalized (§2.9). Accessibility pass (colourblind, reduced motion/flash, contrast).
- [ ] Packaging: Windows `.exe`/installer + macOS `.dmg` notarised, GitHub Releases.
- Subagents: (1) Ranks/profiles/ratings, (2) Star-rating + accessibility, (3) Packaging + release prep.
- Done when: global ranks recompute; profiles/ratings live; accessibility toggles work; signed builds export.

---

## Design import notes (Stages 3, 4, 5)
HUD (2A) and Editor (1A) come from the Claude Design MCP (`https://api.anthropic.com/v1/design/mcp`, `/design-login`), project `cc6f9e35-9183-4b42-8d8a-be6dfc135fe1`. **Translate `.dc.html` into Godot Control nodes — never embed HTML.** Consume Stage 0's `DesignTokens`/`octet_theme.tres` so imported and hand-built screens stay consistent. All other screens follow `DESIGN_BRIEF.md` directly.

## Risks / dependencies
- Design MCP connectivity blocks Stages 3–5 fidelity — connect before those chats.
- Rust toolchain availability decides Stage 6 backend — probe at Stage 6.
- Firebase project + web config needed before Stage 7.
- macOS signing/notarisation needs Apple credentials at Stage 8 (kept out of repo).
